{
  "name": "gtm-docs",
  "version": "1.0.0",
  "description": "GTM Documentation Site",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "worker:dev": "npm run docs:build && wrangler dev",
    "deploy": "npm run docs:build && wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vitepress": "^1.5.0",
    "vue": "^3.5.0"
  }
}
